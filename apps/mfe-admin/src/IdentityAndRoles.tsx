import React,{useEffect,useState}from'react';
import{Alert,Button,Card,Form,Input,Modal,Popconfirm,Select,Space,Table,Tag,message}from'antd';
import{adminApi}from'./api';

type Row=Record<string,any>;
const required=[{required:true,message:'این فیلد الزامی است'}];

export function IdentityAndRoles(){
  const[users,setUsers]=useState<Row[]>([]),[groups,setGroups]=useState<Row[]>([]),[accessGroups,setAccessGroups]=useState<Row[]>([]),[roles,setRoles]=useState<Row[]>([]),[assignments,setAssignments]=useState<Row[]>([]),[roleOpen,setRoleOpen]=useState(false);
  const[roleForm]=Form.useForm(),[assignmentForm]=Form.useForm();
  const load=()=>Promise.all([adminApi('/users'),adminApi('/directory-groups'),adminApi('/ou-access/access-groups'),adminApi('/roles'),adminApi('/role-assignments')]).then(([u,g,ag,r,a])=>{setUsers(u);setGroups(g);setAccessGroups(ag);setRoles(r);setAssignments(a)}).catch(error=>message.error(error.message));
  useEffect(()=>{void load()},[]);
  const createRole=async(values:Row)=>{try{await adminApi('/roles',{method:'POST',body:JSON.stringify(values)});setRoleOpen(false);roleForm.resetFields();await load();message.success('نقش کاربردی ایجاد شد')}catch(error){message.error((error as Error).message)}};
  const assign=async(values:Row)=>{try{await adminApi('/role-assignments',{method:'POST',body:JSON.stringify({...values,expiresAt:values.expiresAt||null})});assignmentForm.resetFields();await load();message.success('نقش تخصیص یافت')}catch(error){message.error((error as Error).message)}};
  const subjectOptions=(type:string)=>(type==='DIRECTORY_GROUP'?groups:type==='ACCESS_GROUP'?accessGroups:users).map(item=>({value:item.id,label:type==='DIRECTORY_GROUP'?`${item.display_name} — ${item.normalized_path}`:type==='ACCESS_GROUP'?`${item.name} — ${item.code}`:`${item.display_name??item.username} — ${item.username}`}));
  return <Space direction="vertical" size={16} style={{width:'100%'}}><Alert showIcon type="info" message="گروه‌های سازمانی نقش نیستند" description="گروه‌ها از Keycloak/LDAP همگام و فقط‌خواندنی‌اند. نقش‌های کاربردی بسته‌های قابلیت هستند و می‌توان آن‌ها را به کاربر یا گروه تخصیص داد."/><Card title="گروه‌های سازمانی همگام‌شده"><Table rowKey="id" dataSource={groups} pagination={{pageSize:8}} columns={[{title:'نام',dataIndex:'display_name'},{title:'مسیر پایدار',dataIndex:'normalized_path'},{title:'شناسه خارجی',dataIndex:'external_id'},{title:'وضعیت',render:(_,row)=><Tag color={row.status==='ACTIVE'?'green':'default'}>{row.status}</Tag>},{title:'آخرین همگام‌سازی',dataIndex:'sync_at',render:value=>value?new Date(value).toLocaleString('fa-IR'):'—'}]}/></Card><Card title="نقش‌های کاربردی" extra={<Button type="primary" onClick={()=>setRoleOpen(true)}>نقش جدید</Button>}><Table rowKey="id" dataSource={roles} pagination={false} columns={[{title:'کلید نقش',dataIndex:'role_key'},{title:'نام فارسی',dataIndex:'name_fa'},{title:'نام انگلیسی',dataIndex:'name_en'},{title:'وضعیت',dataIndex:'status'}]}/></Card><Card title="تخصیص نقش"><Form form={assignmentForm} layout="inline" onFinish={assign} initialValues={{subjectType:'USER'}}><Form.Item name="subjectType" rules={required}><Select style={{width:180}} options={[{value:'USER',label:'کاربر'},{value:'DIRECTORY_GROUP',label:'گروه LDAP'},{value:'ACCESS_GROUP',label:'گروه OU'}]}/></Form.Item><Form.Item noStyle shouldUpdate={(before,after)=>before.subjectType!==after.subjectType}>{()=>{const type=assignmentForm.getFieldValue('subjectType');return <Form.Item name="subjectId" rules={required}><Select showSearch optionFilterProp="label" style={{width:340}} placeholder={type==='USER'?'انتخاب کاربر':'انتخاب گروه'} options={subjectOptions(type)}/></Form.Item>}}</Form.Item><Form.Item name="roleId" rules={required}><Select style={{width:260}} placeholder="نقش" options={roles.filter(role=>role.status==='ACTIVE').map(role=>({value:role.id,label:`${role.name_fa} (${role.role_key})`}))}/></Form.Item><Button type="primary" htmlType="submit">تخصیص</Button></Form><Table style={{marginTop:16}} rowKey={row=>`${row.subject_type}:${row.subject_id}:${row.role_id}`} dataSource={assignments} pagination={{pageSize:8}} columns={[{title:'نوع',dataIndex:'subject_type',render:value=>value==='USER'?'کاربر':value==='ACCESS_GROUP'?'گروه OU':'گروه LDAP'},{title:'کاربر/گروه',dataIndex:'subject_name'},{title:'نقش',dataIndex:'role_key'},{title:'انقضا',dataIndex:'expires_at',render:value=>value?new Date(value).toLocaleString('fa-IR'):'بدون انقضا'},{title:'',render:(_,row)=><Popconfirm title="این تخصیص لغو شود؟" onConfirm={()=>adminApi(`/role-assignments/${row.subject_type}/${row.subject_id}/${row.role_id}`,{method:'DELETE'}).then(load).catch(error=>message.error(error.message))}><Button danger>لغو</Button></Popconfirm>}]}/></Card><Modal open={roleOpen} title="تعریف نقش کاربردی" onCancel={()=>setRoleOpen(false)} onOk={()=>roleForm.submit()}><Form form={roleForm} layout="vertical" onFinish={createRole}><Form.Item name="roleKey" label="کلید پایدار نقش" rules={required}><Input placeholder="hr-supervisor"/></Form.Item><Form.Item name="nameFa" label="نام فارسی" rules={required}><Input/></Form.Item><Form.Item name="nameEn" label="نام انگلیسی" rules={required}><Input/></Form.Item></Form></Modal></Space>;
}
